let str = "  Kamlesh mehta mehta  ";

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.length); // gives the length of the string

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.slice(0,7)); //gives the slices output string from 0 to 7 && if passed one argeument it will remove string to that number (< number).

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.substring(0,7)); // gives the string out of it

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.charCodeAt(0)); // returns the ASCII value of that number

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.concat("sonal")); // returns the concated string

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.endsWith("z")); // returns if the the string ends with the particular value returns true or false.

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(String.fromCharCode(65)); // returns that as 65 is the unicode value of A

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.includes("mehta")); // returns true or false fro whether string is present or not

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.indexOf("mehta"));  // starting index of the string

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.lastIndexOf('mehta'));  // last string of the matched one returning first index of it

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.localeCompare("Kamlesh mehta mehta")); // return 1 if sorted after return 0 if sorted before returns 0 if mathces

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.match(/Kam/gi)); // returns the matched data as an array

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.repeat(3)); // repeats the string number of times

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.search(/mehta/gi)); // returns the matched data using normal string or regular expressions

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.slice(0, 1)); // returns string less than the 2nd number

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.split("a")); converts the string in array as by splitting it

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.startsWith("Kamlesh",10)); // searhes the string if matched returns true and 2 nd argument is search from which position

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.substr(0,2)); // returns the sliced string less than 2nd argument.

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.substring(0,2)); // returns the sliced string less than 2nd argument.

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.toLocaleLowerCase()); // returns the lower case with string
// console.log(str.toLocaleUpperCase()); // returns the upper case with string

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.toLowerCase()); // returns the lower case string
// console.log(str.toUpperCase()); // returns the lower case string

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.toString()); // converts to string 

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.trim()); // returns the string without any white space

// -------------------------------------------------------------------------------------------------------------------------------------------

// console.log(str.valueOf()); // returns the primitive value of a string
